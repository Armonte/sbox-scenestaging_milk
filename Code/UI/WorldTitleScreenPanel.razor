@using Sandbox;
@using Sandbox.UI;
@using Bowling;
@inherits PanelComponent
@namespace Sandbox

<root class="world-title-screen">
	<div class="header">
		<h1>BOWLING</h1>
		<p>Choose your game mode</p>
	</div>
	
	<div class="menu-options">
		<button class="menu-button" @onclick="StartSolo">
			<span class="button-icon">🎳</span>
			<span class="button-text">Solo</span>
		</button>
		
		<button class="menu-button" @onclick="StartMultiplayer">
			<span class="button-icon">👥</span>
			<span class="button-text">Multiplayer</span>
		</button>
		
		<button class="menu-button" @onclick="ShowOptions">
			<span class="button-icon">⚙️</span>
			<span class="button-text">Options</span>
		</button>
		
		<button class="menu-button" @onclick="ExitGame">
			<span class="button-icon">🚪</span>
			<span class="button-text">Exit</span>
		</button>
	</div>
</root>

@code {
	BowlingGameFlowManager FlowManager => Scene?.GetAllComponents<BowlingGameFlowManager>().FirstOrDefault();
	
	void StartSolo()
	{
		Log.Info("Starting solo game...");
		FlowManager?.StartSoloGame();
	}
	
	void StartMultiplayer()
	{
		Log.Info("Starting multiplayer game...");
		FlowManager?.StartMultiplayerGame();
	}
	
	void ShowOptions()
	{
		Log.Info("Options menu - TODO: Implement");
		// TODO: Show options panel
	}
	
	void ExitGame()
	{
		Log.Info("Exiting game...");
		// TODO: Proper exit handling
	}
	
	protected override int BuildHash()
	{
		return HashCode.Combine(FlowManager);
	}
}

